/**
 * The brand tagline, once. It renders as the homepage hero eyebrow (redesign),
 * the homepage h1 (gate-off), the Organization.slogan in the homepage schema,
 * and the alt text of the shared social card. Four surfaces, one string, so a
 * future change cannot leave a retired line behind on one of them: the
 * previous tagline survived in four variants across those surfaces.
 *
 * The period is part of the line.
 */
export const BRAND_TAGLINE = 'Find the game, plan the night.';
